package com.lunfit.workoutservice.entity;

/**
 * Supported workout types.
 * Phase 1: RUN only.
 * Phase 2+: CYCLING, SWIM (with corresponding metrics extension tables).
 */
public enum WorkoutType {
    RUN
}
